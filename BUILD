How to build Instrument Trainer
===============================================

'QT', 'make', and 'CMake' are dependencies of Instrument Trainer and are required to build it.

$ cd build/
$ cmake ../src
$ make

This will leave the compiled application in 'build/pianobooster'
